#include <stdio.h>
#include "main.h"
int mul(int a, int b);

/**
 * mul - that multiplies two integers
 * @a: first integer
 * #b: second integer
 * Return: 0
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
