#include <stdio.h>
#include "main.h"
int _isalpha(int c);
/**
 * _isalpha - checks for alphabetic character
 * @c: contains letter to be compared
 * Returns 1 if letter is lowercase or uppercase
 * Return 0 otherwise
 * Return: Always 0 (sucess)
 */
int _isalpha(int c)
{
	if ((c > 'a' && c < 'z') || (c > 'A' && c < 'Z')
		return (1)
	else
		return (0);
