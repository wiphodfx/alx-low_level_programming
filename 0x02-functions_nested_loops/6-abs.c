#include <stdio.h>
#include "main.h"
int _abs(int);
/**
 * _abs - computes the absolute values of integers
 * int - parameter to hold value
 * Return: Always 0 (success)
 */
int _abs(int)
{
	if (int % 10 >= 0)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
