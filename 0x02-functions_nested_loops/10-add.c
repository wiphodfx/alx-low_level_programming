#include <stdio.h>
#include "main.h"
int add(int i, int j);
/**
 * add - adds two integers and returns the result;
 * @i: holds holds first number
 * @j: hold second number
 * Return: Always 0 (success)
 */
int add(int i, int j)
{
	return (i + j);
}
