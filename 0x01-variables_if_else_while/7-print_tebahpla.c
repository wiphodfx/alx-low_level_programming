#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints the lowercase alphabets in reverse order
 * followed by a newline
 * Return Always0 (success)
 */
int main(void)
{
	char ck;

	for (ck = 'a'; ck > 'a'; ck--)
	{
		putchar(ck);
	}
	putchar('\n');
	return (0);
}
