#include <stdio.h>

/**
 * _puts - prints a string, followed by a new line
 * to stdout.
 * @str: holds the string
 * Return: nothing
 */
void _puts(char *str)
{
	_puts('p');
	_puts('u');
	_puts('t');
	_puts('s');
	_puts('\n');
}
